# Compression Fixtures

Archive fixture targets for `src/services/integration/compression_handler.rs`.

The `archives/` tree is checked in directly and can be consumed by tests as-is.
Some hardlink-negative tar fixtures are currently placeholder tarballs with README notes
inside the archive and should be replaced with crafted hardlink entries when needed.
